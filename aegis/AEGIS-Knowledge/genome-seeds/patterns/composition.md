# Composition

Prefer composing small, single-purpose functions/objects over deep inheritance hierarchies. Composition keeps behavior explicit and testable in isolation; inheritance hides behavior in a chain that's hard to trace and easy to break by changing a base class.
